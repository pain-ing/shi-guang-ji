图标文件说明：

为了让应用有专业的外观，请准备以下图标文件并放置在 public 文件夹中：

1. icon.ico - Windows 图标文件（至少包含 256x256 尺寸）
2. icon.icns - macOS 图标文件（可选）
3. icon.png - Linux 图标文件（512x512 尺寸，可选）

您可以使用在线工具（如 https://www.icoconverter.com/）将 PNG 图片转换为 ICO 格式。

如果暂时没有图标，应用仍可以正常构建和运行，只是会使用默认的 Electron 图标。